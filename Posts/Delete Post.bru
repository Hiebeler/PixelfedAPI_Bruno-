meta {
  name: Delete Post
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/v1/statuses/{{postId}}
  body: none
  auth: inherit
}

script:post-response {
  if (res.status == 200) {
    bru.setEnvVar("postId","")
  }
}
