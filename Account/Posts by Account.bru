meta {
  name: Posts by Account
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/pixelfed/v1/accounts/{{accountId}}/statuses
  body: none
  auth: inherit
}

query {
  ~limit: 
  ~max_id: 
}

script:post-response {
  bru.setEnvVar("postId",res.body[0].id)
}
